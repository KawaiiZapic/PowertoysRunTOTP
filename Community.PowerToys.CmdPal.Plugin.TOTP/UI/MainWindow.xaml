<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Community.PowerToys.CmdPal.Plugin.TOTP.UI.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Community.PowerToys.CmdPal.Plugin.TOTP.UI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Command Palette Plugin: TOTP"
    >

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TitleBar x:Name="AppTitleBar"
              Title="Command Palette Plugin: TOTP">
            <TitleBar.IconSource>
                <ImageIconSource ImageSource="/Assets/icon.png" />
            </TitleBar.IconSource>
        </TitleBar>

        <Frame x:Name="rootFrame" Grid.Row="1">
            <Grid VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0,-30,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>


                <Image Grid.Column="0" Grid.Row="0" Source="/Assets/add.png" Width="100"/>

                <StackPanel
                Grid.Row="0"
                Grid.Column="1"
                    Grid.ColumnSpan="2"
                Margin="12">
                    <TextBlock
                    Margin="0,0,0,12"
                    Style="{ThemeResource TitleTextBlockStyle}"
                    x:Uid="WelcomeTitle"
                    TextWrapping="WrapWholeWords" />
                    <TextBlock x:Uid="WelcomeDesc" TextWrapping="WrapWholeWords" />
                </StackPanel>
            </Grid>


        </Frame>


        <StackPanel
            Grid.Row="2"
            VerticalAlignment="Bottom"
            HorizontalAlignment="Center"
            Margin="12,80,12,12">
            <TextBlock
                Style="{ThemeResource BodyStrongTextBlockStyle}"
                HorizontalAlignment="Center"
                TextWrapping="WrapWholeWords"
                x:Uid="GetPTTitle"
                />
            <TextBlock HorizontalAlignment="Center">
                <Hyperlink NavigateUri="https://github.com/microsoft/PowerToys/releases/latest">GitHub</Hyperlink>
                <Hyperlink NavigateUri="https://apps.microsoft.com/detail/XP89DCGQ3K6VLD">Microsoft Store</Hyperlink>
            </TextBlock>
        </StackPanel>
    </Grid>
</Window>
